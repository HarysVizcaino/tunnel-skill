{
  "name": "tunnel-agent-skill",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "validate": "npm run check && npm test",
    "pack:skill": "node scripts/package.mjs"
  },
  "license": "MIT"
}
